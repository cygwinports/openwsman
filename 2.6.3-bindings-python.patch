--- origsrc/openwsman-2.6.3/bindings/python/CMakeLists.txt	2016-06-21 06:36:20.000000000 -0500
+++ src/openwsman-2.6.3/bindings/python/CMakeLists.txt	2016-07-03 21:51:39.895869200 -0500
@@ -12,7 +12,7 @@ enable_testing()
 
 add_subdirectory(tests)
 
-set(Python_ADDITIONAL_VERSIONS 3.6 3.5 2.7 2.6)
+set(Python_ADDITIONAL_VERSIONS 3.7 3.6 3.5 2.7 2.6)
 FIND_PACKAGE(PythonLibs)
 IF (PYTHON_LIBRARY)
   FIND_PACKAGE(PythonInterp REQUIRED)
@@ -33,7 +33,7 @@ ADD_CUSTOM_COMMAND (
 SET( CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wall" )
 
 SET(pywsman_SRCS ${SWIG_OUTPUT} ${CMAKE_CURRENT_SOURCE_DIR}/../openwsman.c)
-ADD_LIBRARY( pywsman SHARED ${pywsman_SRCS} )
+ADD_LIBRARY( pywsman MODULE ${pywsman_SRCS} )
 SET_TARGET_PROPERTIES( pywsman PROPERTIES PREFIX "_" )
 
 INCLUDE_DIRECTORIES( ${PYTHON_INCLUDE_DIRS} )
@@ -44,6 +44,7 @@ INCLUDE_DIRECTORIES( ${CMAKE_SOURCE_DIR}
 
 TARGET_LINK_LIBRARIES( pywsman wsman )
 TARGET_LINK_LIBRARIES( pywsman wsman_client )
+TARGET_LINK_LIBRARIES( pywsman ${PYTHON_LIBRARIES} )
 
 INSTALL(TARGETS pywsman LIBRARY DESTINATION ${PYTHON_LIB_DIR})
 INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/pywsman.py DESTINATION ${PYTHON_LIB_DIR} )
